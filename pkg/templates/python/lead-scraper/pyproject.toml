[project]
name = "lead-scraper"
version = "0.1.0"
description = "Google Maps Lead Scraper - A Kernel template for scraping local business leads"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.11.1",
    "kernel>=0.23.0",
    "pydantic>=2.12.5",
]
